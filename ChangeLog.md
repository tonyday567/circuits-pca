# Changelog

## 0.1.0.0 — 2026-07-12

* Initial package: PCA as dagger-compose + `Optic (,)` residual over harpie arrays.
* `Circuit.PCA.Optic` — free-Tambara product packing (lens).
* `Circuit.PCA.Lin` — center, Gram (`X†X`), BLAS mult via `Harpie.Hmatrix`.
* `Circuit.PCA` — `fit` / `scores` / `reconstruct` / `rowLens`.
* Design sibling: `circuits` examples/pca.md.
